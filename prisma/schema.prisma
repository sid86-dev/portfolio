generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("MONGO_URI")
}

model Projects {
  id          String   @id @map("_id")
  description String
  github      String
  image       String
  link        String
  tags        String[]
  title       String
}
